idea for arrayWithIndex

construction(length)
    create array of length length
    set index = 0;
    length = length

methods:
    add(element)
        array[index] = element
        index++;

    getIndex()
        return index
